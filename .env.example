# scitex-gen environment variables
#
# Copy to .env (or source from your shell profile) and adjust values.
# All variables are optional; defaults shown after `=`.

# Base directory for SciTeX cache and state files.
# Used by scitex_gen.cache and related helpers.
SCITEX_DIR=~/.scitex
